import { CommentDirective } from './comment.directive';

describe('CommentDirective', () => {
  it('should create an instance', () => {
    const directive = new CommentDirective();
    expect(directive).toBeTruthy();
  });
});
